# Rust
/target
**/*.rs.bk

# Node
**/node_modules
dist
.vite

# Generated wasm-pack output (rebuilt via `pnpm build:wasm`)
/packages/wasm-sim

# Large data artifacts — committed OUTPUTS only (see scripts/build_data.sh)
*.pmtiles
*.pbf
/data/raw

# Subtitle source art (~140 MB of raw covers) is versioned via git-LFS (see .gitattributes);
# reprocess to the transparent webp with `python3 scripts/build_subtitles.py`.

# Playwright
playwright-report
test-results

# Misc
.DS_Store
*.log
.playwright-mcp/
# WSL alternate-data-stream stubs (downloaded-file security-zone metadata)
*:Zone.Identifier
.claude/
